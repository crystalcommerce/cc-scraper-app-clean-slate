module.exports = function(socketInstance) {
    // get all events;
        // this includes the listeners and emitters
    // get custom emitters
        // these are emitters happening outside of the original socket.io instance
        // we wrap that instance inside a new class that has it's custom emitters allowing it to
}